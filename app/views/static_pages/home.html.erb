<!DOCTYPE html>
<html>
<head>
	<title>HOME PAGE</title>
</head>
<body>
	<div class="jumbotron">
		<% if user_signed_in? %>
			<a class="btn btn-warning" href=<%= edit_user_registration_path %> >Edit </a>
			<%= link_to "Log OUT", destroy_user_session_path , method: :delete, class: "btn btn-danger" %>
		<% else %>
			<a class="btn btn-info" href=<%= new_user_session_path %> >Sign IN - SE CONNECTER</a>
			<a class="btn btn-success" href=<%= new_user_registration_path %> >Sign UP - INSCRIVEZ VOUS</a>
		<% end %>
	</div>

</body>
</html>